# FTECH VIEWER 기본 설정
# 이 파일의 설정값은 클라이언트에 하드코딩됩니다.
# 서버 배포 후 아래 값을 실제 서버 주소로 변경하세요.

# ============================================
# 서버 설정
# ============================================

# ID/Rendezvous 서버 주소
# 형식: 도메인:포트 또는 IP:포트
# rendezvous_server = "your-server.ftech.com"

# Relay 서버 주소 (선택사항, 비워두면 rendezvous_server와 동일)
# relay_server = ""

# 서버 공개키 (서버 배포 후 data/id_ed25519.pub 값 입력)
# key = "서버에서_생성된_공개키_입력"

# ============================================
# 클라이언트 설정
# ============================================

# 기본 언어 (ko: 한국어)
# lang = "ko"

# 다크 모드 (dark, light, system)
# theme = "system"

